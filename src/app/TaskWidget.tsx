"use client"

import React, { useEffect, useRef, useState } from "react";

export default function TaskWidget() {

    return (
        <div className="bg-base-300 aspect-square m-8 p-4 rounded-3xl">
            <span className="font-bold text-lg">Task</span>
        </div>
    );
}